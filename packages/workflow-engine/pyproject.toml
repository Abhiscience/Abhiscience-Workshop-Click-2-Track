[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "workflow_engine"
version = "0.1.0"
description = "Workflow engine package"
requires-python = ">=3.11"
dependencies = [
    "pandas",
]

[tool.setuptools.packages.find]
where = ["."]
