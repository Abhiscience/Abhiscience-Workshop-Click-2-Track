# Add these to your `.env` if they are not already present.
#
# If app_installations.push_token contains Expo push tokens, set:
#   PUSH_PROVIDER=expo
#
# Placeholder keys for FCM are included to keep the helper usable if the mobile
# app switches from Expo to Firebase in the future.
# PUSH_PROVIDER=expo
